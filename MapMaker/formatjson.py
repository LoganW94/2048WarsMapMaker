import json


class FormatJson:

	__init__(self, description, players, turn_index, map_size, grid):
		self.description = description
		self.players = players
		self.turn_index = turn_index
		self.map_size = map_size
		self.grid = grid

		